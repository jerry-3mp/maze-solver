package io.jistud.mazesolver.server.model;

/**
 * Record representing a position in a maze with row and column coordinates.
 */
public record Position(int row, int col) {
    // No additional methods needed as this is a simple data record
}
